import {SAVE_KEY,readSave,createSave} from './savegame.mjs';
export const LIBRARY_KEY='ashfall-save-library-v1';
export function listSaves(storage){const raw=storage.getItem(LIBRARY_KEY);if(raw){const data=JSON.parse(raw);if(!Array.isArray(data))throw Error('Invalid save library');return data.map(s=>{readSave(s.raw);if(typeof s.id!=='string'||typeof s.name!=='string')throw Error('Invalid save');return s});}const legacy=storage.getItem(SAVE_KEY);if(!legacy)return [];readSave(legacy);return [{id:'legacy',name:'Original save',raw:legacy}];}
export function writeSave(storage,game,name,id=''){name=name.trim().slice(0,48);if(!name)throw Error('Name required');const saves=listSaves(storage),raw=createSave(game);if(id){const index=saves.findIndex(s=>s.id===id);if(index<0)throw Error('Save not found');saves[index]={id,name,raw};}else{if(saves.length>=20)throw Error('Save limit reached');saves.push({id:crypto.randomUUID(),name,raw});}storage.setItem(LIBRARY_KEY,JSON.stringify(saves));return saves;}
